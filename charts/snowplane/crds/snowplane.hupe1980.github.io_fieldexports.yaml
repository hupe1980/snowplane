---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: fieldexports.snowplane.hupe1980.github.io
spec:
  group: snowplane.hupe1980.github.io
  names:
    categories:
    - snowplane
    kind: FieldExport
    listKind: FieldExportList
    plural: fieldexports
    shortNames:
    - fexp
    singular: fieldexport
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.from.resource.kind
      name: Source Kind
      type: string
    - jsonPath: .spec.from.resource.name
      name: Source Name
      type: string
    - jsonPath: .spec.to.kind
      name: Target Kind
      type: string
    - jsonPath: .spec.to.name
      name: Target Name
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          FieldExport copies a field from a Snowplane managed resource's status into a
          ConfigMap or Secret, enabling cross-resource data passing in Kubernetes.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FieldExportSpec defines the desired state of FieldExport.
            properties:
              from:
                description: From specifies the source resource and field path.
                properties:
                  path:
                    description: |-
                      Path is a dot-notation path to the field in the source resource's status.
                      Examples: ".status.showOutput.createdOn", ".status.fullyQualifiedName"

                      Only dot-separated field names are supported. Array indexing
                      (e.g. ".status.conditions[0].message") is not supported.
                    minLength: 1
                    type: string
                  resource:
                    description: Resource references the source Snowplane managed
                      resource.
                    properties:
                      kind:
                        description: Kind is the resource kind (e.g., "Database",
                          "Warehouse", "Schema").
                        minLength: 1
                        type: string
                      name:
                        description: Name is the resource name.
                        minLength: 1
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                required:
                - path
                - resource
                type: object
              to:
                description: To specifies the target ConfigMap or Secret.
                properties:
                  key:
                    description: Key is the key within the ConfigMap data or Secret
                      data to write to.
                    minLength: 1
                    type: string
                  kind:
                    description: 'Kind is the target resource kind: "ConfigMap" or
                      "Secret".'
                    enum:
                    - ConfigMap
                    - Secret
                    type: string
                  name:
                    description: Name is the target ConfigMap or Secret name.
                    minLength: 1
                    type: string
                required:
                - key
                - kind
                - name
                type: object
            required:
            - from
            - to
            type: object
            x-kubernetes-validations:
            - message: spec.from.path must start with '.status.'
              rule: self.from.path.startsWith('.status.')
            - message: spec.from.path does not support array indexing
              rule: '!self.from.path.contains(''['')'
            - message: spec.from.resource.kind must be a supported Snowplane resource
                kind
              rule: self.from.resource.kind in ['Alert','Database','Schema','Warehouse','User','AccountRole','DatabaseRole','AccountRoleGrant','DatabaseRoleGrant','ShareGrant','AccountRoleAssignment','DatabaseRoleAssignment','Table','View','Stage','Task','Stream','Tag','NetworkPolicy','ResourceMonitor','MaskingPolicy','RowAccessPolicy','GrantOwnership','StorageIntegration','SecurityIntegration','FileFormat','Pipe','DynamicTable','PasswordPolicy','NetworkRule']
            - message: spec.from.resource.kind is immutable
              rule: self.from.resource.kind == oldSelf.from.resource.kind
            - message: spec.from.resource.name is immutable
              rule: self.from.resource.name == oldSelf.from.resource.name
            - message: spec.to.kind is immutable
              rule: self.to.kind == oldSelf.to.kind
            - message: spec.to.name is immutable
              rule: self.to.name == oldSelf.to.name
            - message: spec.to.key is immutable
              rule: self.to.key == oldSelf.to.key
          status:
            description: FieldExportStatus defines the observed state of FieldExport.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastExportedValueHash:
                description: |-
                  LastExportedValueHash is a SHA-256 hash of the last exported value.
                  For Secrets, the raw value is never exposed in status.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
