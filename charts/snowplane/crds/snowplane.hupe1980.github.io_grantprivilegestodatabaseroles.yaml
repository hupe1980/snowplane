---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: grantprivilegestodatabaseroles.snowplane.hupe1980.github.io
spec:
  group: snowplane.hupe1980.github.io
  names:
    categories:
    - snowplane
    kind: GrantPrivilegesToDatabaseRole
    listKind: GrantPrivilegesToDatabaseRoleList
    plural: grantprivilegestodatabaseroles
    singular: grantprivilegestodatabaserole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: SYNCED
      type: string
    - jsonPath: .spec.privilege
      name: PRIVILEGE
      type: string
    - jsonPath: .spec.allPrivileges
      name: ALL_PRIV
      type: boolean
    - jsonPath: .spec.databaseRole
      name: ROLE
      type: string
    - jsonPath: .status.kind
      name: KIND
      type: string
    - jsonPath: .spec.providerRef.name
      name: PROVIDER
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          GrantPrivilegesToDatabaseRole is the Schema for the grantprivilegestodatabaseroles API.
          It grants a Snowflake privilege to a database role.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              GrantPrivilegesToDatabaseRoleSpec defines the desired state of a GrantPrivilegesToDatabaseRole.
              All grant fields are immutable after creation — changing any field requires
              a REVOKE + re-GRANT (delete and recreate the resource to change).

              Database roles can only grant on: the parent DATABASE, schemas, or schema
              objects. Unlike account role grants, they cannot grant on ACCOUNT or on
              account-level objects (WAREHOUSE, USER, etc.).
            properties:
              allPrivileges:
                description: |-
                  AllPrivileges grants all privileges on the target.
                  Translates to GRANT ALL PRIVILEGES ON ... TO DATABASE ROLE ...
                  Mutually exclusive with Privilege.
                type: boolean
              databaseRole:
                description: |-
                  DatabaseRole is the fully qualified database role name (e.g. MY_DB.MY_ROLE).
                  Mutually exclusive with DatabaseRoleRef.
                maxLength: 255
                minLength: 1
                type: string
              databaseRoleRef:
                description: |-
                  DatabaseRoleRef references a DatabaseRole CR in the same namespace.
                  When set, the database role name is resolved from the CR's fullyQualifiedName.
                  Mutually exclusive with DatabaseRole.
                properties:
                  name:
                    description: Name of the referent.
                    maxLength: 255
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent. Defaults to the namespace of the
                      referencing resource when omitted, enabling cross-namespace
                      references in platform/project-team enterprise topologies.
                    type: string
                required:
                - name
                type: object
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what happens to the Snowflake resource when the CR is deleted.
                  Defaults to "Delete".
                enum:
                - Delete
                - Orphan
                type: string
              managementPolicies:
                description: ManagementPolicies specifies lifecycle policies for this
                  resource.
                properties:
                  adoptionPolicy:
                    default: fail-if-exists
                    description: |-
                      AdoptionPolicy controls how the reconciler handles a pre-existing
                      Snowflake resource on first reconciliation.
                      Defaults to "fail-if-exists".
                    enum:
                    - adopt
                    - fail-if-exists
                    type: string
                  createOrAlter:
                    default: true
                    description: |-
                      CreateOrAlter controls whether CREATE OR ALTER is used instead of
                      the legacy CREATE + ALTER two-step flow for supported resource types.
                      Defaults to true.
                    type: boolean
                  driftPolicy:
                    default: correct
                    description: |-
                      DriftPolicy controls whether detected drift is corrected or only
                      reported via conditions and events.
                      Defaults to "correct".
                    enum:
                    - correct
                    - detect-only
                    type: string
                type: object
              "on":
                description: |-
                  On defines what the privilege is granted on.
                  Database roles can grant on the parent DATABASE, on schemas, or on schema objects.
                  Exactly one variant must be set.
                properties:
                  database:
                    description: |-
                      Database grants a privilege on the database itself (e.g. USAGE, CREATE SCHEMA).
                      For: ON DATABASE <database_name>
                    maxLength: 255
                    minLength: 1
                    type: string
                  schema:
                    description: Schema grants a privilege on a schema or on all/future
                      schemas in a database.
                    properties:
                      allInDatabase:
                        description: |-
                          AllInDatabase grants on all existing schemas in the specified database.
                          For: ON ALL SCHEMAS IN DATABASE <db_name>
                        maxLength: 255
                        minLength: 1
                        type: string
                      allInDatabaseRef:
                        description: |-
                          AllInDatabaseRef references a Database CR for the ALL SCHEMAS IN DATABASE grant.
                          Mutually exclusive with AllInDatabase.
                        properties:
                          name:
                            description: Name of the referent.
                            maxLength: 255
                            minLength: 1
                            type: string
                          namespace:
                            description: |-
                              Namespace of the referent. Defaults to the namespace of the
                              referencing resource when omitted, enabling cross-namespace
                              references in platform/project-team enterprise topologies.
                            type: string
                        required:
                        - name
                        type: object
                      futureInDatabase:
                        description: |-
                          FutureInDatabase grants on future schemas in the specified database.
                          For: ON FUTURE SCHEMAS IN DATABASE <db_name>
                        maxLength: 255
                        minLength: 1
                        type: string
                      futureInDatabaseRef:
                        description: |-
                          FutureInDatabaseRef references a Database CR for the FUTURE SCHEMAS IN DATABASE grant.
                          Mutually exclusive with FutureInDatabase.
                        properties:
                          name:
                            description: Name of the referent.
                            maxLength: 255
                            minLength: 1
                            type: string
                          namespace:
                            description: |-
                              Namespace of the referent. Defaults to the namespace of the
                              referencing resource when omitted, enabling cross-namespace
                              references in platform/project-team enterprise topologies.
                            type: string
                        required:
                        - name
                        type: object
                      schemaName:
                        description: |-
                          SchemaName is the fully qualified schema name (e.g. "MY_DB"."PUBLIC").
                          The controller uses this verbatim in ON SCHEMA <schema_name>.
                          For: ON SCHEMA <schema_name>
                        maxLength: 255
                        minLength: 1
                        type: string
                      schemaRef:
                        description: |-
                          SchemaRef references a Schema CR in the same namespace.
                          When set, the schema FQN is resolved from the CR's fullyQualifiedName.
                          Mutually exclusive with SchemaName.
                        properties:
                          name:
                            description: Name of the referent.
                            maxLength: 255
                            minLength: 1
                            type: string
                          namespace:
                            description: |-
                              Namespace of the referent. Defaults to the namespace of the
                              referencing resource when omitted, enabling cross-namespace
                              references in platform/project-team enterprise topologies.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of schemaName, schemaRef, allInDatabase,
                        allInDatabaseRef, futureInDatabase, or futureInDatabaseRef
                        must be set
                      rule: '(has(self.schemaName) ? 1 : 0) + (has(self.schemaRef)
                        ? 1 : 0) + (has(self.allInDatabase) ? 1 : 0) + (has(self.allInDatabaseRef)
                        ? 1 : 0) + (has(self.futureInDatabase) ? 1 : 0) + (has(self.futureInDatabaseRef)
                        ? 1 : 0) == 1'
                  schemaObject:
                    description: |-
                      SchemaObject grants a privilege on a specific schema object, or on
                      all/future schema objects of a given type in a database or schema.
                    properties:
                      all:
                        description: |-
                          All specifies granting on all existing objects of a given type.
                          For: ON ALL <type_plural> IN {DATABASE <db> | SCHEMA <schema>}
                        properties:
                          inDatabase:
                            description: InDatabase scopes the grant to all objects
                              of the type in the specified database.
                            maxLength: 255
                            minLength: 1
                            type: string
                          inDatabaseRef:
                            description: |-
                              InDatabaseRef references a Database CR.
                              Mutually exclusive with InDatabase.
                            properties:
                              name:
                                description: Name of the referent.
                                maxLength: 255
                                minLength: 1
                                type: string
                              namespace:
                                description: |-
                                  Namespace of the referent. Defaults to the namespace of the
                                  referencing resource when omitted, enabling cross-namespace
                                  references in platform/project-team enterprise topologies.
                                type: string
                            required:
                            - name
                            type: object
                          inSchema:
                            description: InSchema scopes the grant to all objects
                              of the type in the specified schema.
                            maxLength: 255
                            minLength: 1
                            type: string
                          inSchemaRef:
                            description: |-
                              InSchemaRef references a Schema CR.
                              Mutually exclusive with InSchema.
                            properties:
                              name:
                                description: Name of the referent.
                                maxLength: 255
                                minLength: 1
                                type: string
                              namespace:
                                description: |-
                                  Namespace of the referent. Defaults to the namespace of the
                                  referencing resource when omitted, enabling cross-namespace
                                  references in platform/project-team enterprise topologies.
                                type: string
                            required:
                            - name
                            type: object
                          objectTypePlural:
                            description: ObjectTypePlural is the plural form of the
                              object type.
                            enum:
                            - TABLES
                            - VIEWS
                            - MATERIALIZED VIEWS
                            - STAGES
                            - FILE FORMATS
                            - FUNCTIONS
                            - PROCEDURES
                            - STREAMS
                            - TASKS
                            - PIPES
                            - SEQUENCES
                            - TAGS
                            - MASKING POLICIES
                            - ROW ACCESS POLICIES
                            - ALERTS
                            - SECRETS
                            - MODELS
                            - DYNAMIC TABLES
                            - ICEBERG TABLES
                            - EVENT TABLES
                            - EXTERNAL TABLES
                            - SCHEMAS
                            type: string
                        required:
                        - objectTypePlural
                        type: object
                        x-kubernetes-validations:
                        - message: inDatabase and inDatabaseRef are mutually exclusive
                          rule: '!(has(self.inDatabase) && has(self.inDatabaseRef))'
                        - message: inSchema and inSchemaRef are mutually exclusive
                          rule: '!(has(self.inSchema) && has(self.inSchemaRef))'
                        - message: exactly one scope (database or schema) must be
                            set
                          rule: '(has(self.inDatabase) || has(self.inDatabaseRef)
                            ? 1 : 0) + (has(self.inSchema) || has(self.inSchemaRef)
                            ? 1 : 0) == 1'
                      future:
                        description: |-
                          Future specifies granting on future objects of a given type.
                          For: ON FUTURE <type_plural> IN {DATABASE <db> | SCHEMA <schema>}
                        properties:
                          inDatabase:
                            description: InDatabase scopes the grant to all objects
                              of the type in the specified database.
                            maxLength: 255
                            minLength: 1
                            type: string
                          inDatabaseRef:
                            description: |-
                              InDatabaseRef references a Database CR.
                              Mutually exclusive with InDatabase.
                            properties:
                              name:
                                description: Name of the referent.
                                maxLength: 255
                                minLength: 1
                                type: string
                              namespace:
                                description: |-
                                  Namespace of the referent. Defaults to the namespace of the
                                  referencing resource when omitted, enabling cross-namespace
                                  references in platform/project-team enterprise topologies.
                                type: string
                            required:
                            - name
                            type: object
                          inSchema:
                            description: InSchema scopes the grant to all objects
                              of the type in the specified schema.
                            maxLength: 255
                            minLength: 1
                            type: string
                          inSchemaRef:
                            description: |-
                              InSchemaRef references a Schema CR.
                              Mutually exclusive with InSchema.
                            properties:
                              name:
                                description: Name of the referent.
                                maxLength: 255
                                minLength: 1
                                type: string
                              namespace:
                                description: |-
                                  Namespace of the referent. Defaults to the namespace of the
                                  referencing resource when omitted, enabling cross-namespace
                                  references in platform/project-team enterprise topologies.
                                type: string
                            required:
                            - name
                            type: object
                          objectTypePlural:
                            description: ObjectTypePlural is the plural form of the
                              object type.
                            enum:
                            - TABLES
                            - VIEWS
                            - MATERIALIZED VIEWS
                            - STAGES
                            - FILE FORMATS
                            - FUNCTIONS
                            - PROCEDURES
                            - STREAMS
                            - TASKS
                            - PIPES
                            - SEQUENCES
                            - TAGS
                            - MASKING POLICIES
                            - ROW ACCESS POLICIES
                            - ALERTS
                            - SECRETS
                            - MODELS
                            - DYNAMIC TABLES
                            - ICEBERG TABLES
                            - EVENT TABLES
                            - EXTERNAL TABLES
                            - SCHEMAS
                            type: string
                        required:
                        - objectTypePlural
                        type: object
                        x-kubernetes-validations:
                        - message: inDatabase and inDatabaseRef are mutually exclusive
                          rule: '!(has(self.inDatabase) && has(self.inDatabaseRef))'
                        - message: inSchema and inSchemaRef are mutually exclusive
                          rule: '!(has(self.inSchema) && has(self.inSchemaRef))'
                        - message: exactly one scope (database or schema) must be
                            set
                          rule: '(has(self.inDatabase) || has(self.inDatabaseRef)
                            ? 1 : 0) + (has(self.inSchema) || has(self.inSchemaRef)
                            ? 1 : 0) == 1'
                      objectName:
                        description: |-
                          ObjectName is the fully qualified name of the specific object.
                          Required when granting on a specific object.
                        type: string
                      objectType:
                        description: |-
                          ObjectType is the schema object type.
                          Required when granting on a specific object.
                        enum:
                        - TABLE
                        - VIEW
                        - MATERIALIZED VIEW
                        - STAGE
                        - FILE FORMAT
                        - FUNCTION
                        - PROCEDURE
                        - STREAM
                        - TASK
                        - PIPE
                        - SEQUENCE
                        - TAG
                        - MASKING POLICY
                        - ROW ACCESS POLICY
                        - ALERT
                        - SECRET
                        - MODEL
                        - DYNAMIC TABLE
                        - ICEBERG TABLE
                        - EVENT TABLE
                        - EXTERNAL TABLE
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of (objectType+objectName), all, or future
                        must be set
                      rule: '(has(self.objectType) && has(self.objectName) ? 1 : 0)
                        + (has(self.all) ? 1 : 0) + (has(self.future) ? 1 : 0) ==
                        1'
                type: object
                x-kubernetes-validations:
                - message: exactly one of database, schema, or schemaObject must be
                    set
                  rule: '(has(self.database) && size(self.database) > 0 ? 1 : 0) +
                    (has(self.schema) ? 1 : 0) + (has(self.schemaObject) ? 1 : 0)
                    == 1'
              paused:
                description: |-
                  Paused suspends reconciliation of this resource. When true, the
                  controller skips all Snowflake operations and sets Synced=False
                  with reason ReconcilePaused. The Snowflake resource is not modified
                  or deleted while paused.
                type: boolean
              privilege:
                description: |-
                  Privilege is the Snowflake privilege to grant (e.g. USAGE, SELECT, CREATE TABLE).
                  Mutually exclusive with AllPrivileges.
                maxLength: 255
                type: string
              providerRef:
                description: ProviderRef references the ProviderConfig to use for
                  Snowflake connectivity.
                properties:
                  name:
                    default: default
                    description: Name of the ProviderConfig to use.
                    maxLength: 255
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace of the ProviderConfig. Defaults to the namespace of the
                      referring resource. Set this to reference a ProviderConfig in a
                      different namespace (e.g. a shared system namespace).
                    type: string
                type: object
              useRole:
                description: |-
                  UseRole specifies the Snowflake role to activate via USE ROLE before
                  CREATE/ALTER/DROP operations. In Snowflake's DAC model the role active
                  at CREATE time becomes the object owner, so this field indirectly
                  controls initial ownership.
                  This field is immutable after the resource has been created in Snowflake.
                maxLength: 255
                minLength: 1
                type: string
              withGrantOption:
                default: false
                description: WithGrantOption allows the grantee to re-grant the privilege
                  to other roles.
                type: boolean
            required:
            - "on"
            - providerRef
            type: object
            x-kubernetes-validations:
            - message: spec.privilege is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.privilege) == has(self.privilege) && (!has(self.privilege)
                || self.privilege == oldSelf.privilege)
            - message: spec.allPrivileges is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.allPrivileges) == has(self.allPrivileges) && (!has(self.allPrivileges)
                || self.allPrivileges == oldSelf.allPrivileges)
            - message: spec.on is immutable (delete and recreate the resource to change)
              rule: self.on == oldSelf.on
            - message: spec.databaseRole is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.databaseRole) == has(self.databaseRole) && (!has(self.databaseRole)
                || self.databaseRole == oldSelf.databaseRole)
            - message: spec.databaseRoleRef is immutable (delete and recreate the
                resource to change)
              rule: has(oldSelf.databaseRoleRef) == has(self.databaseRoleRef) && (!has(self.databaseRoleRef)
                || self.databaseRoleRef == oldSelf.databaseRoleRef)
            - message: spec.withGrantOption is immutable (delete and recreate the
                resource to change)
              rule: self.withGrantOption == oldSelf.withGrantOption
            - message: spec.useRole is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.useRole) == has(self.useRole) && (!has(self.useRole)
                || self.useRole == oldSelf.useRole)
            - message: exactly one of spec.databaseRole or spec.databaseRoleRef must
                be set
              rule: (has(self.databaseRole) && !has(self.databaseRoleRef)) || (!has(self.databaseRole)
                && has(self.databaseRoleRef))
            - message: exactly one of spec.privilege or spec.allPrivileges must be
                set
              rule: '(has(self.privilege) && size(self.privilege) > 0 ? 1 : 0) + (has(self.allPrivileges)
                && self.allPrivileges ? 1 : 0) == 1'
          status:
            description: GrantPrivilegesToDatabaseRoleStatus defines the observed
              state of a GrantPrivilegesToDatabaseRole.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              fullyQualifiedName:
                description: FullyQualifiedName is the Snowflake fully qualified identifier
                  for this resource.
                type: string
              kind:
                description: Kind indicates the resolved grant kind (Regular, Future,
                  All).
                type: string
              lastAppliedSpecHash:
                description: |-
                  LastAppliedSpecHash is the SHA-256 hash of the spec as of the last
                  successful reconciliation. The reconciler uses this to distinguish
                  spec changes (user edited the CR) from external drift (Snowflake
                  state diverged without a spec change). This is more reliable than
                  comparing ObservedGeneration vs Generation because metadata-only
                  changes (labels, annotations) increment Generation without changing
                  the spec.
                type: string
              lastReconcileTime:
                description: |-
                  LastReconcileTime is the timestamp of the most recent successful
                  reconciliation. Useful for SLO monitoring and diagnosing whether
                  reconciliation is running for a specific resource.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent metadata.generation
                  observed by the controller.
                format: int64
                type: integer
              showOutput:
                description: ShowOutput contains the raw SHOW GRANTS entry for this
                  grant.
                properties:
                  createdOn:
                    description: CreatedOn is the timestamp when the grant was created.
                    type: string
                  grantOption:
                    description: GrantOption indicates whether WITH GRANT OPTION was
                      used.
                    type: boolean
                  grantedBy:
                    description: GrantedBy is the role that performed the grant.
                    type: string
                  grantedOn:
                    description: 'GrantedOn is the object type (from SHOW GRANTS:
                      "granted_on" or "grant_on" for future grants).'
                    type: string
                  grantedTo:
                    description: GrantedTo is the grantee category (ROLE, DATABASE_ROLE,
                      SHARE).
                    type: string
                  granteeName:
                    description: GranteeName is the role or share name.
                    type: string
                  name:
                    description: Name is the object name.
                    type: string
                  privilege:
                    description: Privilege is the granted privilege name.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
