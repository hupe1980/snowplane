---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: databaseroleassignments.snowplane.hupe1980.github.io
spec:
  group: snowplane.hupe1980.github.io
  names:
    categories:
    - snowplane
    kind: DatabaseRoleAssignment
    listKind: DatabaseRoleAssignmentList
    plural: databaseroleassignments
    shortNames:
    - dra
    singular: databaseroleassignment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: SYNCED
      type: string
    - jsonPath: .spec.databaseRoleName
      name: DB-ROLE
      type: string
    - jsonPath: .spec.toRole
      name: TO-ROLE
      type: string
    - jsonPath: .spec.toDatabaseRole
      name: TO-DB-ROLE
      type: string
    - jsonPath: .spec.providerRef.name
      name: PROVIDER
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          DatabaseRoleAssignment is the Schema for the databaseroleassignments API.
          It assigns a database role to an account role or another database role in Snowflake.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: "DatabaseRoleAssignmentSpec defines the desired state of
              a DatabaseRoleAssignment.\nAssigns a database role to an account role
              or another database role:\n\n\tGRANT DATABASE ROLE <db>.<role> TO ROLE
              <parent_role>\n\tGRANT DATABASE ROLE <db>.<role> TO DATABASE ROLE <db>.<other_role>\n\nAll
              fields are immutable after creation — changing any field requires\ndeleting
              and recreating the resource.\n\nMutual exclusivity rules:"
            properties:
              databaseRoleName:
                description: |-
                  DatabaseRoleName is the fully qualified name of the database role to assign (e.g. MY_DB.MY_ROLE).
                  Mutually exclusive with DatabaseRoleRef.
                type: string
              databaseRoleRef:
                description: |-
                  DatabaseRoleRef references a DatabaseRole CR in the same namespace.
                  When set, the database role name is resolved from the CR's fullyQualifiedName.
                  Mutually exclusive with DatabaseRoleName.
                properties:
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what happens to the Snowflake resource when the CR is deleted.
                  Defaults to "Delete".
                enum:
                - Delete
                - Orphan
                type: string
              managementPolicies:
                description: ManagementPolicies specifies lifecycle policies for this
                  resource.
                properties:
                  adoptionPolicy:
                    default: fail-if-exists
                    description: |-
                      AdoptionPolicy controls how the reconciler handles a pre-existing
                      Snowflake resource on first reconciliation.
                      Defaults to "fail-if-exists".
                    enum:
                    - adopt
                    - fail-if-exists
                    type: string
                  createOrAlter:
                    default: true
                    description: |-
                      CreateOrAlter controls whether CREATE OR ALTER is used instead of
                      the legacy CREATE + ALTER two-step flow for supported resource types.
                      Defaults to true.
                    type: boolean
                  driftPolicy:
                    default: correct
                    description: |-
                      DriftPolicy controls whether detected drift is corrected or only
                      reported via conditions and events.
                      Defaults to "correct".
                    enum:
                    - correct
                    - detect-only
                    type: string
                type: object
              paused:
                description: |-
                  Paused suspends reconciliation of this resource. When true, the
                  controller skips all Snowflake operations and sets Synced=False
                  with reason ReconcilePaused. The Snowflake resource is not modified
                  or deleted while paused.
                type: boolean
              providerRef:
                description: ProviderRef references the ProviderConfig to use for
                  Snowflake connectivity.
                properties:
                  name:
                    default: default
                    description: Name of the ProviderConfig to use.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace of the ProviderConfig. Defaults to the namespace of the
                      referring resource. Set this to reference a ProviderConfig in a
                      different namespace (e.g. a shared system namespace).
                    type: string
                type: object
              toDatabaseRole:
                description: |-
                  ToDatabaseRole is the fully qualified name of the database role to assign to (e.g. MY_DB.MY_ROLE).
                  Mutually exclusive with ToDatabaseRoleRef, ToRole, ToRoleRef.
                type: string
              toDatabaseRoleRef:
                description: |-
                  ToDatabaseRoleRef references a DatabaseRole CR in the same namespace.
                  When set, the target database role name is resolved from the CR's fullyQualifiedName.
                  Mutually exclusive with ToDatabaseRole, ToRole, ToRoleRef.
                properties:
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              toRole:
                description: |-
                  ToRole is the name of the account role to assign the database role to.
                  Mutually exclusive with ToRoleRef, ToDatabaseRole, ToDatabaseRoleRef.
                type: string
              toRoleRef:
                description: |-
                  ToRoleRef references an AccountRole CR in the same namespace.
                  When set, the target role name is resolved from the CR's spec.name.
                  Mutually exclusive with ToRole, ToDatabaseRole, ToDatabaseRoleRef.
                properties:
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              useRole:
                description: |-
                  UseRole specifies the Snowflake role to activate via USE ROLE before
                  CREATE/ALTER/DROP operations. In Snowflake's DAC model the role active
                  at CREATE time becomes the object owner, so this field indirectly
                  controls initial ownership.
                  This field is immutable after the resource has been created in Snowflake.
                minLength: 1
                type: string
            required:
            - providerRef
            type: object
            x-kubernetes-validations:
            - message: spec.databaseRoleName is immutable (delete and recreate the
                resource to change)
              rule: has(oldSelf.databaseRoleName) == has(self.databaseRoleName) &&
                (!has(self.databaseRoleName) || self.databaseRoleName == oldSelf.databaseRoleName)
            - message: spec.databaseRoleRef is immutable (delete and recreate the
                resource to change)
              rule: has(oldSelf.databaseRoleRef) == has(self.databaseRoleRef) && (!has(self.databaseRoleRef)
                || self.databaseRoleRef == oldSelf.databaseRoleRef)
            - message: spec.toRole is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.toRole) == has(self.toRole) && (!has(self.toRole)
                || self.toRole == oldSelf.toRole)
            - message: spec.toRoleRef is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.toRoleRef) == has(self.toRoleRef) && (!has(self.toRoleRef)
                || self.toRoleRef == oldSelf.toRoleRef)
            - message: spec.toDatabaseRole is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.toDatabaseRole) == has(self.toDatabaseRole) && (!has(self.toDatabaseRole)
                || self.toDatabaseRole == oldSelf.toDatabaseRole)
            - message: spec.toDatabaseRoleRef is immutable (delete and recreate the
                resource to change)
              rule: has(oldSelf.toDatabaseRoleRef) == has(self.toDatabaseRoleRef)
                && (!has(self.toDatabaseRoleRef) || self.toDatabaseRoleRef == oldSelf.toDatabaseRoleRef)
            - message: spec.useRole is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.useRole) == has(self.useRole) && (!has(self.useRole)
                || self.useRole == oldSelf.useRole)
            - message: exactly one of spec.databaseRoleName or spec.databaseRoleRef
                must be set
              rule: '(has(self.databaseRoleName) ? 1 : 0) + (has(self.databaseRoleRef)
                ? 1 : 0) == 1'
            - message: exactly one of spec.toRole, spec.toRoleRef, spec.toDatabaseRole,
                or spec.toDatabaseRoleRef must be set
              rule: '(has(self.toRole) ? 1 : 0) + (has(self.toRoleRef) ? 1 : 0) +
                (has(self.toDatabaseRole) ? 1 : 0) + (has(self.toDatabaseRoleRef)
                ? 1 : 0) == 1'
          status:
            description: DatabaseRoleAssignmentStatus defines the observed state of
              a DatabaseRoleAssignment.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              fullyQualifiedName:
                description: FullyQualifiedName is the Snowflake fully qualified identifier
                  for this resource.
                type: string
              lastAppliedSpecHash:
                description: |-
                  LastAppliedSpecHash is the SHA-256 hash of the spec as of the last
                  successful reconciliation. The reconciler uses this to distinguish
                  spec changes (user edited the CR) from external drift (Snowflake
                  state diverged without a spec change). This is more reliable than
                  comparing ObservedGeneration vs Generation because metadata-only
                  changes (labels, annotations) increment Generation without changing
                  the spec.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent metadata.generation
                  observed by the controller.
                format: int64
                type: integer
              showOutput:
                description: ShowOutput contains the raw SHOW GRANTS OF DATABASE ROLE
                  output entry for this assignment.
                properties:
                  createdOn:
                    description: CreatedOn is the timestamp when the role assignment
                      was created.
                    type: string
                  grantedBy:
                    description: GrantedBy is the role that performed the assignment.
                    type: string
                  grantedTo:
                    description: 'GrantedTo is the grantee category: "ROLE", "USER",
                      or "DATABASE_ROLE".'
                    type: string
                  granteeName:
                    description: GranteeName is the name of the role, user, or database
                      role receiving the assignment.
                    type: string
                  role:
                    description: Role is the role name that was assigned.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
