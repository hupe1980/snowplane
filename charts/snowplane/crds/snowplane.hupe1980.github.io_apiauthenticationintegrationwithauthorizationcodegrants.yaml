---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: apiauthenticationintegrationwithauthorizationcodegrants.snowplane.hupe1980.github.io
spec:
  group: snowplane.hupe1980.github.io
  names:
    categories:
    - snowplane
    kind: APIAuthenticationIntegrationWithAuthorizationCodeGrant
    listKind: APIAuthenticationIntegrationWithAuthorizationCodeGrantList
    plural: apiauthenticationintegrationwithauthorizationcodegrants
    shortNames:
    - aaiwacg
    singular: apiauthenticationintegrationwithauthorizationcodegrant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: SYNCED
      type: string
    - jsonPath: .spec.name
      name: SNOWFLAKE-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          APIAuthenticationIntegrationWithAuthorizationCodeGrant is the Schema for the Snowflake
          API Authentication Security Integration with Authorization Code Grant API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              APIAuthenticationIntegrationWithAuthorizationCodeGrantSpec defines the desired state
              of a Snowflake API Authentication Security Integration using the OAuth2
              authorization code grant flow.
            properties:
              comment:
                description: Comment for the integration.
                type: string
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what happens to the Snowflake resource when the CR is deleted.
                  Defaults to "Delete".
                enum:
                - Delete
                - Orphan
                type: string
              enabled:
                description: Specifies whether this security integration is enabled
                  or disabled.
                type: boolean
              managementPolicies:
                description: ManagementPolicies specifies lifecycle policies for this
                  resource.
                properties:
                  adoptionPolicy:
                    default: fail-if-exists
                    description: |-
                      AdoptionPolicy controls how the reconciler handles a pre-existing
                      Snowflake resource on first reconciliation.
                      Defaults to "fail-if-exists".
                    enum:
                    - adopt
                    - fail-if-exists
                    type: string
                  createOrAlter:
                    default: true
                    description: |-
                      CreateOrAlter controls whether CREATE OR ALTER is used instead of
                      the legacy CREATE + ALTER two-step flow for supported resource types.
                      Defaults to true.
                    type: boolean
                  driftPolicy:
                    default: correct
                    description: |-
                      DriftPolicy controls whether detected drift is corrected or only
                      reported via conditions and events.
                      Defaults to "correct".
                    enum:
                    - correct
                    - detect-only
                    type: string
                type: object
              name:
                description: Snowflake integration identifier (e.g. 'MY_API_AUTH_ACG').
                minLength: 1
                type: string
              oauthAccessTokenValidity:
                description: Specifies the default lifetime of the OAuth access token
                  (in seconds).
                type: integer
              oauthAllowedScopes:
                description: Specifies the list of scopes to use during the OAuth
                  authorization code flow.
                items:
                  type: string
                type: array
              oauthAuthorizationEndpoint:
                description: Specifies the URL for authenticating to the external
                  service.
                type: string
              oauthClientAuthMethod:
                description: Specifies how client credentials are sent to the external
                  service.
                enum:
                - CLIENT_SECRET_BASIC
                - CLIENT_SECRET_POST
                type: string
              oauthClientId:
                description: Specifies the client ID for the OAuth application in
                  the external service.
                type: string
              oauthClientSecret:
                description: Specifies the client secret for the OAuth application
                  in the external service.
                type: string
              oauthRefreshTokenValidity:
                description: Specifies the validity of the refresh token obtained
                  from the OAuth server.
                type: integer
              oauthTokenEndpoint:
                description: Specifies the token endpoint used by the client to obtain
                  an access token.
                type: string
              paused:
                description: |-
                  Paused suspends reconciliation of this resource. When true, the
                  controller skips all Snowflake operations and sets Synced=False
                  with reason ReconcilePaused. The Snowflake resource is not modified
                  or deleted while paused.
                type: boolean
              providerRef:
                description: ProviderRef references the ProviderConfig to use for
                  Snowflake connectivity.
                properties:
                  name:
                    default: default
                    description: Name of the ProviderConfig to use.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace of the ProviderConfig. Defaults to the namespace of the
                      referring resource. Set this to reference a ProviderConfig in a
                      different namespace (e.g. a shared system namespace).
                    type: string
                type: object
              useRole:
                description: |-
                  UseRole specifies the Snowflake role to activate via USE ROLE before
                  CREATE/ALTER/DROP operations. In Snowflake's DAC model the role active
                  at CREATE time becomes the object owner, so this field indirectly
                  controls initial ownership.
                  This field is immutable after the resource has been created in Snowflake.
                minLength: 1
                type: string
            required:
            - enabled
            - name
            - oauthClientId
            - oauthClientSecret
            - providerRef
            type: object
            x-kubernetes-validations:
            - message: spec.name is immutable (delete and recreate)
              rule: self.name == oldSelf.name
          status:
            description: APIAuthenticationIntegrationWithAuthorizationCodeGrantStatus
              defines the observed state.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              describeOutput:
                description: Output from DESCRIBE INTEGRATION.
                properties:
                  authType:
                    type: string
                  comment:
                    type: string
                  enabled:
                    type: string
                  oauthAccessTokenValidity:
                    type: string
                  oauthAllowedScopes:
                    type: string
                  oauthAuthorizationEndpoint:
                    type: string
                  oauthClientAuthMethod:
                    type: string
                  oauthClientId:
                    type: string
                  oauthGrant:
                    type: string
                  oauthRefreshTokenValidity:
                    type: string
                  oauthTokenEndpoint:
                    type: string
                  parentIntegration:
                    type: string
                type: object
              fullyQualifiedName:
                description: FullyQualifiedName is the Snowflake fully qualified identifier
                  for this resource.
                type: string
              lastAppliedSpecHash:
                description: |-
                  LastAppliedSpecHash is the SHA-256 hash of the spec as of the last
                  successful reconciliation. The reconciler uses this to distinguish
                  spec changes (user edited the CR) from external drift (Snowflake
                  state diverged without a spec change). This is more reliable than
                  comparing ObservedGeneration vs Generation because metadata-only
                  changes (labels, annotations) increment Generation without changing
                  the spec.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent metadata.generation
                  observed by the controller.
                format: int64
                type: integer
              showOutput:
                description: Output from SHOW SECURITY INTEGRATIONS.
                properties:
                  category:
                    type: string
                  comment:
                    type: string
                  createdOn:
                    type: string
                  enabled:
                    type: string
                  integrationType:
                    type: string
                  name:
                    type: string
                type: object
              trackedParameters:
                description: Tracked parameters for UNSET operations.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
