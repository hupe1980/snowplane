version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - bodyclose
    - errcheck
    - errorlint
    - exhaustive
    - forbidigo
    - goconst
    - gosec
    - govet
    - ineffassign
    - misspell
    - noctx
    - prealloc
    - revive
    - staticcheck
    - unused

  settings:
    revive:
      rules:
        - name: exported
          arguments:
            - disableStutteringCheck
    goconst:
      min-len: 3
      min-occurrences: 6
    forbidigo:
      forbid:
        - pattern: \.UnsafeRaw$
          msg: >-
            UnsafeRaw bypasses SQL parameterization. Each call site must be
            reviewed for injection safety. Add //nolint:forbidigo with a
            justification comment if the usage has been validated.

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - goconst
          - revive
          - gosec
          - errorlint
          - noctx
          - forbidigo
      - path: validation\.go
        linters:
          - goconst
      - path: hack/
        linters:
          - gosec
      # strings.Builder.WriteString never returns an error (always nil)
      - linters:
          - gosec
        source: "WriteString"

formatters:
  enable:
    - gofmt
    - goimports

  settings:
    goimports:
      local-prefixes:
        - github.com/hupe1980/snowplane
