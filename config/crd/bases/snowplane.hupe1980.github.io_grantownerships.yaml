---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: grantownerships.snowplane.hupe1980.github.io
spec:
  group: snowplane.hupe1980.github.io
  names:
    categories:
    - snowplane
    kind: GrantOwnership
    listKind: GrantOwnershipList
    plural: grantownerships
    shortNames:
    - gow
    singular: grantownership
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: SYNCED
      type: string
    - jsonPath: .spec.objectType
      name: OBJECT-TYPE
      type: string
    - jsonPath: .spec.objectName
      name: OBJECT-NAME
      type: string
    - jsonPath: .spec.providerRef.name
      name: PROVIDER
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GrantOwnership is the Schema for the grantownerships API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              GrantOwnershipSpec defines the desired state of a GrantOwnership.
              All fields are immutable after creation — changing any field requires
              deleting and recreating the resource.

              Mutual exclusivity rules:
            properties:
              accountRole:
                description: |-
                  AccountRole is the name of the account role to transfer ownership to.
                  Mutually exclusive with AccountRoleRef, DatabaseRole, and DatabaseRoleRef.
                minLength: 1
                type: string
              accountRoleRef:
                description: |-
                  AccountRoleRef references an AccountRole CR in the same namespace.
                  Mutually exclusive with AccountRole, DatabaseRole, and DatabaseRoleRef.
                properties:
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              currentGrantsBehavior:
                description: |-
                  CurrentGrantsBehavior specifies how existing outbound privileges
                  on the object are handled during the ownership transfer.
                  COPY: preserves existing privileges with the new owner as grantor.
                  REVOKE: removes all existing outbound privileges before transfer.
                  If not set, Snowflake requires that no outbound privileges exist.
                enum:
                - COPY
                - REVOKE
                type: string
              databaseRole:
                description: |-
                  DatabaseRole is the fully qualified database role name (e.g. "MY_DB"."MY_ROLE").
                  Mutually exclusive with AccountRole, AccountRoleRef, and DatabaseRoleRef.
                minLength: 1
                type: string
              databaseRoleRef:
                description: |-
                  DatabaseRoleRef references a DatabaseRole CR in the same namespace.
                  Mutually exclusive with AccountRole, AccountRoleRef, and DatabaseRole.
                properties:
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what happens to the Snowflake resource when the CR is deleted.
                  Defaults to "Delete".
                enum:
                - Delete
                - Orphan
                type: string
              managementPolicies:
                description: ManagementPolicies specifies lifecycle policies for this
                  resource.
                properties:
                  adoptionPolicy:
                    default: fail-if-exists
                    description: |-
                      AdoptionPolicy controls how the reconciler handles a pre-existing
                      Snowflake resource on first reconciliation.
                      Defaults to "fail-if-exists".
                    enum:
                    - adopt
                    - fail-if-exists
                    type: string
                  createOrAlter:
                    default: true
                    description: |-
                      CreateOrAlter controls whether CREATE OR ALTER is used instead of
                      the legacy CREATE + ALTER two-step flow for supported resource types.
                      Defaults to true.
                    type: boolean
                  driftPolicy:
                    default: correct
                    description: |-
                      DriftPolicy controls whether detected drift is corrected or only
                      reported via conditions and events.
                      Defaults to "correct".
                    enum:
                    - correct
                    - detect-only
                    type: string
                type: object
              objectName:
                description: |-
                  ObjectName is the fully qualified name of the object
                  (e.g. "MY_DB" for a database, "MY_DB"."MY_SCHEMA"."MY_TABLE" for a table).
                minLength: 1
                type: string
              objectType:
                description: ObjectType is the Snowflake object type whose ownership
                  is being transferred.
                enum:
                - DATABASE
                - SCHEMA
                - TABLE
                - VIEW
                - MATERIALIZED VIEW
                - WAREHOUSE
                - STAGE
                - FILE FORMAT
                - FUNCTION
                - PROCEDURE
                - STREAM
                - TASK
                - PIPE
                - SEQUENCE
                - TAG
                - MASKING POLICY
                - ROW ACCESS POLICY
                - NETWORK POLICY
                - RESOURCE MONITOR
                - USER
                - COMPUTE POOL
                - INTEGRATION
                - CONNECTION
                - FAILOVER GROUP
                - REPLICATION GROUP
                - EXTERNAL VOLUME
                - ALERT
                - SECRET
                - MODEL
                - DYNAMIC TABLE
                - ICEBERG TABLE
                - EVENT TABLE
                - EXTERNAL TABLE
                - NETWORK RULE
                - PASSWORD POLICY
                type: string
              paused:
                description: |-
                  Paused suspends reconciliation of this resource. When true, the
                  controller skips all Snowflake operations and sets Synced=False
                  with reason ReconcilePaused. The Snowflake resource is not modified
                  or deleted while paused.
                type: boolean
              providerRef:
                description: ProviderRef references the ProviderConfig to use for
                  Snowflake connectivity.
                properties:
                  name:
                    default: default
                    description: Name of the ProviderConfig to use.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace of the ProviderConfig. Defaults to the namespace of the
                      referring resource. Set this to reference a ProviderConfig in a
                      different namespace (e.g. a shared system namespace).
                    type: string
                type: object
              useRole:
                description: |-
                  UseRole specifies the Snowflake role to activate via USE ROLE before
                  CREATE/ALTER/DROP operations. In Snowflake's DAC model the role active
                  at CREATE time becomes the object owner, so this field indirectly
                  controls initial ownership.
                  This field is immutable after the resource has been created in Snowflake.
                minLength: 1
                type: string
            required:
            - objectName
            - objectType
            - providerRef
            type: object
            x-kubernetes-validations:
            - message: spec.objectType is immutable (delete and recreate the resource
                to change)
              rule: self.objectType == oldSelf.objectType
            - message: spec.objectName is immutable (delete and recreate the resource
                to change)
              rule: self.objectName == oldSelf.objectName
            - message: spec.accountRole is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.accountRole) == has(self.accountRole) && (!has(self.accountRole)
                || self.accountRole == oldSelf.accountRole)
            - message: spec.accountRoleRef is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.accountRoleRef) == has(self.accountRoleRef) && (!has(self.accountRoleRef)
                || self.accountRoleRef == oldSelf.accountRoleRef)
            - message: spec.databaseRole is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.databaseRole) == has(self.databaseRole) && (!has(self.databaseRole)
                || self.databaseRole == oldSelf.databaseRole)
            - message: spec.databaseRoleRef is immutable (delete and recreate the
                resource to change)
              rule: has(oldSelf.databaseRoleRef) == has(self.databaseRoleRef) && (!has(self.databaseRoleRef)
                || self.databaseRoleRef == oldSelf.databaseRoleRef)
            - message: spec.useRole is immutable (delete and recreate the resource
                to change)
              rule: has(oldSelf.useRole) == has(self.useRole) && (!has(self.useRole)
                || self.useRole == oldSelf.useRole)
            - message: exactly one of spec.accountRole, spec.accountRoleRef, spec.databaseRole,
                or spec.databaseRoleRef must be set
              rule: '(has(self.accountRole) ? 1 : 0) + (has(self.accountRoleRef) ?
                1 : 0) + (has(self.databaseRole) ? 1 : 0) + (has(self.databaseRoleRef)
                ? 1 : 0) == 1'
          status:
            description: GrantOwnershipStatus defines the observed state of a GrantOwnership.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              fullyQualifiedName:
                description: FullyQualifiedName is the Snowflake fully qualified identifier
                  for this resource.
                type: string
              lastAppliedSpecHash:
                description: |-
                  LastAppliedSpecHash is the SHA-256 hash of the spec as of the last
                  successful reconciliation. The reconciler uses this to distinguish
                  spec changes (user edited the CR) from external drift (Snowflake
                  state diverged without a spec change). This is more reliable than
                  comparing ObservedGeneration vs Generation because metadata-only
                  changes (labels, annotations) increment Generation without changing
                  the spec.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent metadata.generation
                  observed by the controller.
                format: int64
                type: integer
              roleName:
                description: RoleName is the resolved name of the target role.
                type: string
              showOutput:
                description: |-
                  ShowOutput contains the most recently observed SHOW GRANTS output
                  for the OWNERSHIP privilege.
                properties:
                  createdOn:
                    description: CreatedOn is the timestamp when the ownership was
                      granted.
                    type: string
                  grantedOn:
                    description: GrantedOn is the object type (e.g. DATABASE, TABLE).
                    type: string
                  grantedTo:
                    description: GrantedTo is the grantee type (ROLE or DATABASE_ROLE).
                    type: string
                  granteeName:
                    description: GranteeName is the name of the role that owns the
                      object.
                    type: string
                  name:
                    description: Name is the fully qualified object name.
                    type: string
                  privilege:
                    description: Privilege is always OWNERSHIP.
                    type: string
                type: object
              trackedParameters:
                description: TrackedParameters tracks which optional spec fields have
                  been actively SET.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
