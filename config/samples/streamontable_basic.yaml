apiVersion: snowplane.hupe1980.github.io/v1alpha1
kind: StreamOnTable
metadata:
  name: my-stream-on-table
spec:
  name: MY_STREAM_ON_TABLE
  providerRef:
    name: default
  databaseRef:
    name: my-database
  schemaRef:
    name: my-schema
  table: MY_SOURCE_TABLE
  appendOnly: true
  comment: "Stream on table managed by Snowplane"
